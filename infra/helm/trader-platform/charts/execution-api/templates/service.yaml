apiVersion: v1
kind: Service
metadata:
  name: {{ include "execution-api.fullname" . }}
  labels:
    {{- include "execution-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "execution-api.selectorLabels" . | nindent 4 }}
